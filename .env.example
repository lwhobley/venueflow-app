EXPO_PUBLIC_CONVEX_URL=https://your-deployment-name.convex.cloud

# Set to true only after a real in-app purchase provider is wired up
# (see lib/a0-purchases-stub.tsx). When false/unset, the app runs free-tier
# and the SubscriptionGate never locks users out.
EXPO_PUBLIC_BILLING_ENABLED=false
